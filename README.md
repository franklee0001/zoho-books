# Zoho Books/Invoice Scripts

## Setup
1) Copy `.env.example` to `.env` and fill in values.
2) Keep `.env` local only (never commit).

Required keys:
- ZOHO_ACCOUNTS_URL
- ZOHO_CLIENT_ID
- ZOHO_CLIENT_SECRET
- ZOHO_REDIRECT_URI
- ZOHO_REFRESH_TOKEN
- ZOHO_ORG_ID

Optional:
- ZOHO_API_DOMAIN
